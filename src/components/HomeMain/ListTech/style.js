import styled from "styled-components";

export const StyledDivListTech = styled.div`
  background:var(--color-grey-3);
  padding:1rem;
  border-radius:var(--border-default);
    ul{
      display:flex;
      flex-direction:column;
      gap:1rem;
    }
`